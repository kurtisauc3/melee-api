{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Melee API",
            "type": "node",
            "request": "launch",
            "protocol": "inspector",
            "preLaunchTask": "Build.All",
            "cwd": "${workspaceFolder}"
        }

    ]
}
